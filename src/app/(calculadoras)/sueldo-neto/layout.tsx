import { buildCalculatorMetadata } from '@/features/calculators/metadata';
export const metadata = buildCalculatorMetadata('sueldo-neto');
export { CalculatorRouteLayout as default } from '@/features/calculators/metadata';
